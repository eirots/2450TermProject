#include <iostream>

#include "../../uvsimulator.h"

// Just a test to mess with all the basics
int main() {
  /*

    // Create a UVSimulator object
    UVSimulator simulator;

    // Build a program using user input
    std::vector<int> program = simulator.buildProgram();

    // Load the built program into memory
    simulator.loadProgram(program);

    // Execute the loaded program
    simulator.executeProgram();

    // Print the memory contents after execution
    simulator.printMemory();
  */
  return 0;
}
